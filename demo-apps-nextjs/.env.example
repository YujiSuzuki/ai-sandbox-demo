# Copy this file to .env and fill in the values

PORT=3000
NODE_ENV=development

# Path to secret file inside the Docker container. Leave unset for local dev
# (npm run dev) — the app falls back to the repo-relative ./secrets/demo-secret.key.
# DEMO_SECRET_PATH=/app/secrets/demo-secret.key
